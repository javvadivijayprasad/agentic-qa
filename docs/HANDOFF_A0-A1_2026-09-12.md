# Handoff — agentic-qa steps A0 + A1 (2026-09-12)

**For:** Vijay (review + push) · **From:** Claude session (package track)
**Repo folder:** `E:\EB1A_Research\EB1_Master\06_Authorship\Paper_I_AgenticQA\Experiment\agentic-qa\`

## What was done

- A0 scaffold and A1 (types, ledger, replay, CLI, fixture) per `PLAN_agentic-qa_v0.1.md` §1.
- Verified in the build sandbox: `tsc --noEmit` clean under `strict` + `noUncheckedIndexedAccess`;
  27 tests green; `aqa replay examples/fixture-ledger` renders 34 rows; CLI bundled and exercised
  as a real entry (`--version`, missing-path exit 1); Prettier clean.
- NOT verified in the sandbox (npm registry blocked there): `npm ci`, `tsup` build, `vitest` and
  `eslint` themselves. Tests were run through node's test runner with a vitest-compatible shim.
  Expect them to pass unchanged under real vitest; if any assertion helper differs, the fix is in
  `test/`, not `src/`.

## What you do

1. Open a terminal in the repo folder and run, in order:
   ```
   git init -b main
   npm install
   npm run typecheck && npm test && npm run lint && npm run build
   node dist/cli.js replay examples/fixture-ledger
   ```
   All four should be green. `npm install` will create `package-lock.json` — commit it.
2. Confirm `.env` is present and NOT shown by `git status` (it is in `.gitignore`).
3. Review the diff. Files to look at first: `src/types.ts` (the contract), `src/ledger/ledger.ts`,
   `examples/fixture-ledger/events.jsonl` (what the platform agent will build against).
4. First commit + push to `github.com/javvadivijayprasad/agentic-qa` (create the repo private for now;
   public at v0.1.0).
5. Tell the platform agent the fixture is available:
   `examples/fixture-ledger/emit.mjs` — usage in its README (handoff B2 depends on it).

## Decisions taken (flag if you disagree)

- ESLint **flat config** (v9) instead of `.eslintrc` — the pw-extensions repos predate v9; this is the
  current default and what CI will run.
- Ledger parser **throws** on any malformed line rather than skipping — a ledger is evidence.
- `renderLine` strings are part of the contract (platform run-page messages); tests pin them.
- Fixture run ends with 3 passed / 1 failed scenario on purpose, so the platform UI has a failure to show.

## Next (A2)

Loop with stubs: `runtime/loop.ts` (plan→act→observe→verify, budgets, stop rules), `model.ts` behind
an interface with a `ScriptedModel`, `mcp/client.ts` behind an interface with `StubTools`; golden-file
test producing the fixture ledger from a scripted run. One reviewable diff.
